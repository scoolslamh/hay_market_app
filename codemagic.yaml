workflows:
  ios-workflow:
    name: iOS Clean Build

    environment:
      flutter: stable
      xcode: latest

    scripts:
      - name: Clean
        script: flutter clean

      - name: Get packages
        script: flutter pub get

      - name: Build iOS (الحل النهائي 🔥)
        script: flutter build ios --release --no-codesign